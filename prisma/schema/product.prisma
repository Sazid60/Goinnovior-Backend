model Product {
    id          String   @id @default(uuid())
    name        String
    description String
    maxPrice    Float
    minPrice    Float
    quantity    Int
    image       String
    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt

    @@map("products")
}
